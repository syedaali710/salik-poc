[project]
name = "salic-ai-insights"
version = "0.1.0"
description = "SALIC AI Insights — grounded financial chat, avatar, and PPTX report export"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.111.0",
    "uvicorn==0.30.1",
    "python-multipart==0.0.9",
    "requests>=2.32.0",
    "python-pptx==0.6.23",
]

[tool.uv]
package = false
